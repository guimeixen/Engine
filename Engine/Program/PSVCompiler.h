#pragma once

#include <string>

namespace Engine
{
	class PSVCompiler
	{
	public:
		void Compile(const std::string &curProjectDir, const std::string &appName, const std::string &appTitleID);

	private:

	};
}
